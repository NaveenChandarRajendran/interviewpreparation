AB testing is also known as split testing or bucket testing.

Where two version of webpage will created and used in production for an period of time. Later we can find which version produce more user interactive and success rate.